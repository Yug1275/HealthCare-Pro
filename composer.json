{
    "name": "healthcare-pro/app",
    "description": "Healthcare Management System",
    "type": "project",
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {},
    "scripts": {
        "post-install-cmd": [
            "php -r \"if (!file_exists('config/database.php')) { copy('config/database.example.php', 'config/database.php'); }\""
        ]
    }
}
